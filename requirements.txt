# Haunt Verifier worker — Python 3.11+
fastapi>=0.110
uvicorn[standard]>=0.29
# 1.x is required, not just preferred: messages.parse() does not exist in the
# 0.x line the old ">=0.49" pin resolved to, so the classifier fails at import-
# time-plus-one on an old resolve. 1.x also needs Python >= 3.10 (we target 3.11)
# and moves the SDK's own HTTP layer to httpx2 — which coexists with the httpx
# below, used directly by the crawlers. No httpx object crosses the SDK boundary.
anthropic>=1.3,<2
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
Pillow>=10.3
pytesseract>=0.3.10       # OPTIONAL boost; requires the Tesseract binary on the host
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.6
pydantic-settings>=2.2
